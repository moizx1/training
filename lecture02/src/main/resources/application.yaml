spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:news
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

api:
  security:
    ignored: /api/v1/users,/api/v1/users/**